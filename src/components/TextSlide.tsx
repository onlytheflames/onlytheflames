"use client";

import gsap from "gsap";
import { useGSAP } from "@gsap/react";
import { useState } from "react";

const TextSlide = () => {
  return (
    <div>
      <h1 className="uppercase text-white font-druk-wide-web-bold text-[7vw]">
        onlytheflames.
      </h1>
    </div>
  );
};

export default TextSlide;
